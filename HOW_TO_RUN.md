# 🚀 如何运行项目

## 方法1: 使用启动脚本（推荐）

### Windows用户
1. 双击 `START.bat` 文件
2. 脚本会自动检查Node.js、安装依赖并启动服务器

### Mac/Linux用户
1. 打开终端
2. 运行:
   ```bash
   chmod +x START.sh
   ./START.sh
   ```

---

## 方法2: 手动运行

### 前置要求
- **Node.js**: v18.0.0 或更高版本
  - 检查安装: `node --version`
  - 下载: https://nodejs.org/

### 步骤

#### 1. 打开终端/命令提示符

**Windows:**
- 按 `Win + R`，输入 `cmd`，回车
- 或者在文件资源管理器中，在项目文件夹地址栏输入 `cmd` 并回车

**Mac/Linux:**
- 打开 Terminal

#### 2. 进入项目目录

```bash
cd integrated-english-platform
```

如果路径包含中文导致问题，可以：
- 使用引号: `cd "C:\Users\热那克孜420\Downloads\7\integrated-english-platform"`
- 或者直接拖拽文件夹到终端窗口

#### 3. 安装依赖

```bash
npm install
```

**如果安装很慢，可以使用国内镜像:**
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

#### 4. 启动开发服务器

```bash
npm run dev
```

#### 5. 访问应用

打开浏览器访问: **http://localhost:3000**

---

## 方法3: 使用VS Code

### 1. 在VS Code中打开项目
- 打开VS Code
- File → Open Folder
- 选择 `integrated-english-platform` 文件夹

### 2. 打开集成终端
- 按 `` Ctrl + ` `` (Windows) 或 `` Cmd + ` `` (Mac)
- 或者 Terminal → New Terminal

### 3. 运行命令
```bash
npm install
npm run dev
```

---

## 常见问题

### ❌ 错误: 'npm' 不是内部或外部命令

**原因**: Node.js 未安装或未添加到 PATH

**解决方案**:
1. 从 https://nodejs.org/ 下载并安装 Node.js
2. 安装时确保勾选 "Add to PATH" 选项
3. 重启终端或重启电脑

### ❌ 错误: 端口3000已被占用

**解决方案**:
```bash
# 使用其他端口
npm run dev -- -p 3001
```

或关闭占用3000端口的程序

### ❌ 错误: 路径包含中文字符

**解决方案**:
1. 将项目移动到不包含中文的路径（如 `D:\projects\integrated-english-platform`）
2. 或者使用文件资源管理器在项目文件夹中直接打开终端

### ❌ 错误: 依赖安装失败

**解决方案**:
```bash
# 清除缓存
npm cache clean --force

# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json  # Mac/Linux
# 或
rmdir /s node_modules  # Windows
del package-lock.json  # Windows

# 重新安装
npm install
```

### ❌ 错误: Module not found

**解决方案**:
```bash
# 重新安装依赖
npm install
```

---

## 项目命令

```bash
# 开发模式（热重载）
npm run dev

# 构建生产版本
npm run build

# 运行生产版本
npm start

# 代码检查
npm run lint
```

---

## 配置API密钥（可选）

如果需要使用AI功能，创建 `.env.local` 文件：

```env
OPENAI_API_KEY=your_openai_api_key_here
GROQ_API_KEY=your_groq_api_key_here
```

**注意**: 项目可以在没有API密钥的情况下运行，但AI功能将无法使用。

---

## 成功运行的标志

当你看到以下信息时，说明项目运行成功：

```
▲ Next.js 15.0.0
- Local:        http://localhost:3000
- Ready in X.Xs
```

然后在浏览器中打开 http://localhost:3000 即可看到应用！

---

## 需要帮助？

如果遇到问题，请检查：
1. ✅ Node.js 版本（v18+）
2. ✅ npm 版本
3. ✅ 网络连接
4. ✅ 端口是否被占用
5. ✅ 路径中是否包含特殊字符

